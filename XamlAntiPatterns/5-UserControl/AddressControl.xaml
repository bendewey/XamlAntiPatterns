<UserControl x:Class="XamlAntiPatterns._5_UserControl.AddressControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <GroupBox x:Name="GroupContainer" Margin="4">
        <StackPanel Margin="4">
            <TextBlock Text="Address1" />
            <TextBox Text="{Binding Address1, Mode=TwoWay}" />
            <TextBlock Text="Address2" />
            <TextBox Text="{Binding Address2, Mode=TwoWay}" />
            <TextBlock Text="City" />
            <TextBox Text="{Binding City, Mode=TwoWay}" />
            <TextBlock Text="State" />
            <TextBox Text="{Binding State, Mode=TwoWay}" />
            <TextBlock Text="Zip" />
            <TextBox Text="{Binding Zip, Mode=TwoWay}" />
        </StackPanel>
    </GroupBox>
</UserControl>
